# Sidecar overrides for 02-conn-struct
# Generated — edit freely to customize narration and effects.
# Use `python -m pipeline parse ../tutorial/02-conn-struct.md` to see block indices.

blocks:
  0:
    narration: >
      Welcome to Step 2. We'll create a Conn struct to represent HTTP requests
      and responses, and build a parser to turn raw TCP data into structured
      Elixir data.
  1:
    narration: >
      Let's start with: What We're Building.
  11:
    narration: >
      Let's look at the code. Think of it as a blueprint.
    effect: "line_highlight"
    highlight_lines: [1, 2, 3]
  13:
    narration: >
      Let's look at the code. Conn{}.
    effect: "line_highlight"
    highlight_lines: [1, 2, 3]
  15:
    narration: >
      Let's look at the code. Unlike plain maps, structs catch typos at
      compile time.
  18:
    narration: >
      Let's look at the code. alias creates a shortcut so you don't have to
      type the full module name.
    effect: "line_highlight"
    highlight_lines: [1, 4, 6]
  27:
    narration: >
      Let's look at the code. When reading headers, we use a common Elixir
      pattern — passing an accumulator through recursive calls.
    effect: "line_highlight"
    highlight_lines: [1, 2, 3]
  32:
    narration: >
      Let's look at the code. The |> (pipe) operator passes the result of one
      function as the first argument to the next.
    effect: "line_highlight"
    highlight_lines: [2, 5]
  36:
    narration: >
      Let's look at the code. Each field represents a piece of the
      request/response lifecycle.
    effect: "line_highlight"
    highlight_lines: [1, 3, 4]
  39:
    narration: >
      Let's look at the code. Here is the key function — see below for the
      complete file.
    effect: "line_highlight"
    highlight_lines: [1, 2, 3]
  43:
    narration: >
      Let's look at the code. The server now uses the parser.
    effect: "line_highlight"
    highlight_lines: [1, 2, 4]
  51:
    narration: >
      Here's how this looks in bash. The parser is the bridge between the
      network layer (bytes on a wire) and the application layer (Elixir data
      structures).
  56:
    narration: >
      Take a look at this diagram. It illustrates the parser is the bridge
      between the network layer (bytes on a wire) and the application layer
      (elixir data structures).
  63:
    narration: >
      Let's look at the code. You'll be able to write clean, declarative
      routes like.
