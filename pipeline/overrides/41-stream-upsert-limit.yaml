# Sidecar overrides for 41-stream-upsert-limit
# Generated — edit freely to customize narration and effects.
# Use `python -m pipeline parse ../tutorial/41-stream-upsert-limit.md` to see block indices.

blocks:
  0:
    narration: >
      Welcome to Step 41. We'll enhance streams with upsert support and
      client-side limits for efficient list rendering.
  1:
    narration: >
      Let's start with: What We're Building.
  5:
    narration: >
      Let's look at the code. After this step, you can write.
    effect: "line_highlight"
    highlight_lines: [2, 5]
  11:
    narration: >
      Let's look at the code. When stream_insert is called, it checks.
  15:
    narration: >
      Let's look at the code. Returns true or false.
  19:
    narration: >
      Let's look at the code. Removes the first occurrence of an element from
      a list.
  23:
    narration: >
      Let's look at the code. Elixir allows guard clauses on function heads to
      add conditions beyond pattern matching.
    effect: "line_highlight"
    highlight_lines: [1, 3]
  27:
    narration: >
      Let's look at the code. take/2 returns the first N elements (or last N
      with negative).
  29:
    narration: >
      Let's look at the code. drop/2 returns everything except the first N (or
      last N).
  34:
    narration: >
      Let's look at the code. ex — add limit and order fields to the struct.
    effect: "line_highlight"
    highlight_lines: [1, 2, 3]
  42:
    narration: >
      Let's look at the code. ex — the key change in stream_insert.
    effect: "line_highlight"
    highlight_lines: [1, 2, 5]
  46:
    narration: >
      Let's look at the code. ex — add the apply_limit helper.
    effect: "line_highlight"
    highlight_lines: [1, 3, 4]
  57:
    narration: >
      Let's look at the code. ex — add limit: 20 to the stream init and an
      "Update Latest" button.
    effect: "line_highlight"
    highlight_lines: [2, 5, 6]
  61:
    narration: >
      Let's look at the code. js.
    effect: "line_highlight"
    highlight_lines: [2, 3, 4]
  70:
    narration: >
      Here's how this looks in bash. It already checks for existing elements
      by ID and morphs them.
